<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        A bridge binds a socket, and without this it fails with a SecurityException that reads
        like a bug in us. PLAN.md M3 says the SDK README must say so; this is the example saying
        it out loud. It is in the main manifest rather than a debug one because a permission that
        appears only in debug builds is a difference between the two variants that has bitten
        every project that tried it.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="nitrite dbinspect example">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
